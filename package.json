{
  "name": "sh-erp",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "wms-backend",
    "wms-frontend"
  ],
  "scripts": {
    "dev:backend": "npm run start:dev --workspace=wms-backend",
    "dev:frontend": "npm run dev --workspace=wms-frontend",
    "dev": "concurrently -c \"cyan.bold,magenta.bold\" -n \"BACKEND,FRONTEND\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build:backend": "npm run build --workspace=wms-backend",
    "build:frontend": "npm run build --workspace=wms-frontend",
    "build": "npm run build:backend && npm run build:frontend",
    "start:prod": "npm run start:prod --workspace=wms-backend",
    "db:migrate:prod": "npm run migration:run --workspace=wms-backend",
    "check": "node scripts/pre-flight-check.mjs",
    "start-safe": "npm run check && npm run dev",
    "db:backup": "bash scripts/db-backup.sh",
    "db:clean": "node scripts/db-clean.mjs",
    "prepare": "husky"
  },
  "lint-staged": {
    "wms-frontend/src/**/*.{ts,tsx}": [
      "eslint --fix"
    ],
    "wms-backend/src/**/*.ts": [
      "eslint --fix"
    ]
  },
  "devDependencies": {
    "chalk": "^5.6.2",
    "husky": "^9.1.7",
    "is-port-reachable": "^4.0.0",
    "lint-staged": "^16.4.0"
  }
}
