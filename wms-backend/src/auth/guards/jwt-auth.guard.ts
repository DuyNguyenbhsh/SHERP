/**
 * @deprecated Import từ '../../common/guards/jwt-auth.guard' hoặc '../../common'
 * File này chỉ re-export để backward-compatible.
 */
export { JwtAuthGuard } from '../../common/guards/jwt-auth.guard';
